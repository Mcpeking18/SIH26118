package com.mrpl.wristband.cv

import org.opencv.core.Mat
import org.opencv.imgproc.Imgproc

class OpenCVTest {
    fun testOpenCV() {
        val mat = Mat()
        // Simple test to ensure Imgproc is resolved
        // Imgproc.cvtColor(...) would be used here
    }
}
